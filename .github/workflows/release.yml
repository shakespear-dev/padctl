name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  create-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate changelog
        env:
          TAG: ${{ github.ref_name }}
        run: |
          PREV=$(git tag --sort=-version:refname | grep -v "^${TAG}$" | head -1)
          {
            echo "# Changelog — ${TAG}"
            echo
            if [ -n "${PREV}" ]; then
              git log --oneline "${PREV}..${TAG}"
            else
              git log --oneline "${TAG}"
            fi
          } > CHANGELOG.md

      - name: Create release
        env:
          TAG: ${{ github.ref_name }}
          GH_TOKEN: ${{ github.token }}
        run: >
          gh release create "${TAG}"
          --title "${TAG}"
          --notes-file CHANGELOG.md
          --draft=false

  gen-install-artifacts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: mlugg/setup-zig@v2
        with:
          version: 0.15.2

      - name: Build native
        run: zig build -Doptimize=ReleaseSafe -Dlibusb=false

      - name: Generate install artifacts
        run: ./zig-out/bin/padctl install --destdir staging --prefix /usr

      - name: Upload install artifacts
        uses: actions/upload-artifact@v4
        with:
          name: install-artifacts
          path: staging/

  build:
    needs: [create-release, gen-install-artifacts]
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target: [x86_64-linux-musl, aarch64-linux-musl]

    steps:
      - uses: actions/checkout@v4

      - uses: mlugg/setup-zig@v2
        with:
          version: 0.15.2

      - name: Download install artifacts
        uses: actions/download-artifact@v4
        with:
          name: install-artifacts
          path: staging/

      - name: Build
        run: zig build -Doptimize=ReleaseSafe -Dtarget=${{ matrix.target }} -Dlibusb=false

      - name: Package
        env:
          TAG: ${{ github.ref_name }}
          TARGET: ${{ matrix.target }}
        run: |
          ARCHIVE=padctl-${TAG}-${TARGET}.tar.gz
          STAGING=padctl-${TAG}-${TARGET}
          mkdir -p "${STAGING}/bin" "${STAGING}/install"
          for bin in padctl padctl-capture padctl-debug; do
            [ -f "zig-out/bin/${bin}" ] && cp "zig-out/bin/${bin}" "${STAGING}/bin/"
          done
          cp staging/usr/lib/systemd/system/*.service "${STAGING}/install/"
          cp staging/usr/lib/udev/rules.d/*.rules "${STAGING}/install/"
          [ -f staging/usr/bin/padctl-reconnect ] && cp staging/usr/bin/padctl-reconnect "${STAGING}/install/"
          cp -r devices "${STAGING}/"
          cp LICENSE README.md "${STAGING}/"
          tar czf "${ARCHIVE}" "${STAGING}"
          echo "ARCHIVE=${ARCHIVE}" >> "$GITHUB_ENV"

      - name: Upload tarball
        env:
          TAG: ${{ github.ref_name }}
          GH_TOKEN: ${{ github.token }}
        run: gh release upload "${TAG}" "${{ env.ARCHIVE }}" --clobber

      - name: Upload artifact for checksums
        uses: actions/upload-artifact@v4
        with:
          name: archive-${{ matrix.target }}
          path: ${{ env.ARCHIVE }}

  checksums:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download archives
        uses: actions/download-artifact@v4
        with:
          pattern: archive-*
          merge-multiple: true

      - name: Generate SHA256SUMS.txt
        run: sha256sum padctl-*.tar.gz > SHA256SUMS.txt

      - name: Upload checksums
        env:
          TAG: ${{ github.ref_name }}
          GH_TOKEN: ${{ github.token }}
        run: gh release upload "${TAG}" SHA256SUMS.txt --clobber
