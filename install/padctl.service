[Unit]
Description=padctl gamepad compatibility daemon
After=local-fs.target

[Service]
Type=simple
ExecStart=/usr/bin/padctl --config-dir /usr/share/padctl/devices/
Restart=on-failure
RestartSec=3

[Install]
WantedBy=multi-user.target
